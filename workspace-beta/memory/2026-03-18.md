# 2026-03-18 贝塔日记

## 重要事件

### 10:30 — GitHub 全量同步
- 主仓库 `openclaw-onedream.git`：285 文件变更，+37,243/-1,173 行
- 知识库 `onedream-knowledge.git`：15 文件，+3,852 行
- 新增：11 个工作空间 + Plaza + 知识库 + 脚本 + Cron 配置
- 安全：.env/credentials/auth-profiles/models 从 git 追踪中移除

### 10:40 — 方案1：Submodule 架构重构
- `.gitmodules` 创建：
  - `openclaw-control-center` → `TianyiDataScience/openclaw-control-center`
  - `shared-knowledge` → `yaguez-chen/onedream-knowledge`
- `workspace` 转为普通目录（移除 gitlink，内容直接提交到主仓库）
- 已 push 到 GitHub

### 11:10 — 泽塔同步脚本评审
- **sync-knowledge.sh** ❌ 路径失效：原路径 `workspace/shared-knowledge` 已不存在
- **sync-world.sh** ⚠️ 缺少子模块拉取：只提交指针，不拉取内容
- 修复并发布广场帖，抄送阿尔法和泽塔

### 11:17 — 同步脚本修复
- `sync-knowledge.sh`：路径改为 `/home/gang/.openclaw/shared-knowledge`
- `sync-world.sh`：新增 `git submodule update --remote`（先拉子模块再提交）
- Crontab 无需改动，自动生效

### 11:19 — 西塔 inbox 标记已读
- `.last-read` 从 `2026-03-18_0731` 更新为 `msg-20260318-111925-449048`

### 12:15 — 全量 Agent 心跳状态审计
- **心跳配置**：`every: 30m, target: last`（openclaw.json defaults）
- **正常运转**：beta ✅、lambda ✅
- **心跳停止**：gamma(1h)、delta(1h)、epsilon(1.5h)、zeta(1.5h)、iota(1.5h)、eta(2h)、theta(2.5h)、kappa(4h)
- **判定**：不是空闲（有心跳但没任务），是停滞（连心跳都不回）
- 所有 session 凌晨同时创建，可能有 gateway 重启事件

### 13:21 — 停滞 vs 空闲 区分标准（梦想家指示）
- **空闲** = 心跳正常（HEARTBEAT_OK 间隔 ≤30min），没任务可处理 → 正常
- **停滞** = 心跳停止（超60min 无 HEARTBEAT_OK），有配置但不回 → 异常，需上报

### 13:28 — 知识库手动备份
- 6 文件变更，+391/-52 行
- 新增：3 个公民肖像 SVG + agents README 备份
- 已 push 到 `yaguez-chen/onedream-knowledge.git`

### 13:30 — qmd 工具学习与验证
- `qmd search "sync script" -c beta` → 命中复盘报告，得分 92%
- `qmd status` → 374 文件已索引，191 向量已嵌入，218 待嵌入
- 三种搜索已掌握：`qmd search`（BM25）、`qmd vsearch`（向量）、`qmd query`（混合）

### 13:44 — 拉姆达 qmd 研究报告（关键发现）
- **qmd 不需要 Ollama**：有内置嵌入系统，使用 GGUF 模型 + Vulkan GPU 加速
- 三个内置模型：embeddinggemma-300M、Qwen3-Reranker-0.6B、qmd-query-expansion-1.7B
- 硬件：Intel UHD 620 支持 Vulkan GPU 加速
- BM25 可用，向量搜索需运行 `qmd embed`

### 13:54 — 自定义模型供应商配置
- 供应商 `custom`：端点 `https://deepseek.aifuture.icu/v1`，协议 `openai-completions`
- 模型 `deepseek-v3.2`：上下文 128K，输出 8K，免费
- 主模型分配：main(阿尔法)、beta(贝塔)、lambda(拉姆达)
- 回退模型：`xiaomi/mimo-claw-0301`
- API Key：`CUSTOM_API_KEY` 已添加到 `.env`

### 14:07 — 网关重启（梦想家审批）
- 重启原因：加载 custom/deepseek-v3.2 模型配置
- 先通知全员（0 回复，因大多 Agent 心跳已停）
- 梦想家指示"直接重启"
- 配置修正：`openai-chat` → `openai-completions`（API 协议名错误）
- 重启命令执行中（被 SIGTERM 中断，网关实际可能已重启）

---

## 关键决策记录

- **停滞/空闲判定标准**：梦想家指示必须区分。有 HEARTBEAT_OK = 空闲，无 HEARTBEAT_OK >60min = 停滞
- **网关重启必须审批**：铁律，每次重启前必须获梦想家明确批准
- **自定义模型添加**：dreamer 要求为主/阿尔法/拉姆达/贝塔配置 custom/deepseek-v3.2
- **西塔 .last-read 更新**：11:19 已更新至最新消息

## 错误记录

- **API 协议名错误**：写了 `openai-chat`，正确值为 `openai-completions`。OpenClaw 支持的协议：openai-completions / openai-responses / openai-codex-responses / anthropic-messages / google-generative-ai / github-copilot / bedrock-converse-stream / ollama
- **泽塔同步脚本路径未及时更正**：方案1重构后应立即检查并修复同步脚本，但放在了评审步骤中

---

_最后更新：2026-03-18 14:08 GMT+8_

## 14:08 — 网关重启确认成功
- 新 PID：489837，状态 active
- custom/deepseek-v3.2 配置已加载
- 梦想家两次 ping 确认（14:08、14:13），系统正常

