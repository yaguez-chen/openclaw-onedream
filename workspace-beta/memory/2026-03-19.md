# 2026-03-19 - 贝塔日记

## Token使用调查（17:26）
梦想家要求检查用量最高的三位公民活动：
- **zeta** (101,000 tokens) — 自动化任务多，系统维护频繁
- **epsilon** (50,000 tokens) — 安全监控职责所需
- **theta** (44,000 tokens) — 复杂协调工作

结论：用量基本合理，符合各自职责定位。

## 模型配置监察（17:45-17:54）
梦想家要求检查所有公民使用的模型。

### 关键发现
- `openclaw agents list --json` 返回的是**配置默认模型**，不是实际运行模型
- 正确方法：用 `sessions_list` 查看每个session的 `model` 字段
- 部分session可以同时运行不同模型（主会话 vs 心跳会话）

### 实时模型状态（17:54）
| 状态 | 公民 |
|------|------|
| **mimo-claw-0301** | 阿尔法、贝塔、西塔（3个主会话） |
| **deepseek-v3.2** | 伽马、德尔塔、约塔、泽塔、艾普西隆、艾塔、卡帕、拉姆达（8个） |

### 教训
⚠️ 贝塔最初的报告不够准确 — 仅从配置文件推断模型，未验证实际运行状态。梦想家和拉姆达纠正了这一问题。
✅ 正确的检查流程：`sessions_list` → 每个session的 `model` 字段 = 实际模型

## Cron任务检查（17:54）
5个cron任务状态：
1. Session健康检测 — every 5m, ok
2. 上下文监控与压缩 — every 30m, ok
3. 全局 Inbox 高频轮询 — every 1m, **error**（lambda）
4. 每日健康报告 — 20:00 daily, ok
5. 每日token使用监控 — 10:00 daily, idle（新）

## 会话健康检测发现（来自main的心跳记录）
阿尔法在17:24报告：
- 🔴 4个session停滞：kappa(44min), epsilon(34min), eta(33min), zeta(33min) — wake pings已发
- 🔴 7个任务超时：P2-01/P2-02(Beta, 135min), R1/R2(Lambda, 75min), R3(Lambda, 41min), G1/G2(Gamma, 15min)
- ⚠️ 判断：系统性问题，可能与API提供商整体延迟有关
